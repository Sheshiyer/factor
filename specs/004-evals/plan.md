# Plan

Files: scripts/check_company.py, evals/checks.md, .github/workflows/test.yml.

Failure path: The check names the amount and the draft. The GitHub Action fails when that fixture fails.

Implement only the tasks in tasks.md. Do not widen the spec in the same change.
