# Company skills

A skill here is one repeated workflow for this company, as a `SKILL.md` Hermes can load.

Keep framework skills (how every company is operated) on the `factor` profile. Keep company skills here, and install them into that profile only when this company uses them:

```bash
hermes -p factor skills install <path-to-skill>
```

Read the skill before installing it.
