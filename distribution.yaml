name: factor
version: 0.5.0
description: "One employee, three doors, rooms that stay quiet. Claude writes. Codex builds. Jev picks and scores."
hermes_requires: ">=0.21.0"
author: Thoughtseed
license: MIT
distribution_owned:
  - SOUL.md
  - config.yaml
  - skills
  - distribution.yaml
env_requires:
  - name: COMPOSIO_API_KEY
    description: "Only if a company enables the Composio connector. Leave unset otherwise."
    required: false
    default: ""
